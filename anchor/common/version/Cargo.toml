[package]
name = "version"
version = "0.1.0"
edition = { workspace = true }
authors = ["Sigma Prime <contact@sigmaprime.io>"]

[dependencies]
git-version = "0.3.9"
target_info = "0.1.0"

[dev-dependencies]
regex = "1.11"
