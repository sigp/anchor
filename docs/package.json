{
  "name": "anchor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync-version": "node sync-version.js",
    "prebuild": "npm run sync-version",
    "dev": "npm run sync-version && vocs dev",
    "build": "vocs build",
    "preview": "vocs preview"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "vocs": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "typescript": "latest"
  }
}
